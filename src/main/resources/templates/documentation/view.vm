#if ($issue)
    #set($cf_10200 = $customFieldManager.getCustomFieldObject('customfield_10200'))
    #set($cf_10100 = $customFieldManager.getCustomFieldObject('customfield_10100'))
    #set($cf_10000 = $customFieldManager.getCustomFieldObject('customfield_10000'))
    #set($issueDocKey = $cf_10200.getValue($issue))
    #set($issueDoc = $issueManager.getIssueObject($issueDocKey))
    <p>Issue number: $issueKey</p>
    <ol>
        <li>
            Текущая задача
            <ol>
                <li>
                    <p>Value of cf_10000: $number.format($cf_10000.getValue($issue).toString()) </p>
                </li>
                #if($cf_10100.getValue($issue).toString())
                <li>
                    <p>Value of cf_10100: $number.format($cf_10100.getValue($issue).toString()) </p>
                </li>
                #end
            </ol>
        </li>
        #if($issueDocKey)
        <li>
            Смежная задача
            <ol>
                <li>
                    <a href="$baseURL/browse/$issueDocKey"> $issueDocKey </a>
                </li>
                #if($cf_10000.getValue($issueDoc))
                <li>
                    <p>Сумма договора: $number.format($cf_10000.getValue($issueDoc).toString()) </p>
                </li>
                #end
            </ol>
        #end
        </li>
    </ol>

#end